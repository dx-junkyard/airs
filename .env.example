# ===========================================
# データベース設定（必須）
# ===========================================

# PostgreSQL 接続文字列
# connection_limit: コネクションプールの最大接続数（サーバーレス環境では小さめに）
# pool_timeout: プールからコネクション取得のタイムアウト（秒）
DATABASE_URL=postgresql://user:password@localhost:5432/airs?sslmode=require&connection_limit=10&pool_timeout=30

# ===========================================
# 画像ストレージ設定
# ===========================================

# 画像ストレージプロバイダ（vercel-blob | gcs）
# デフォルト: vercel-blob
IMAGE_STORAGE_PROVIDER=vercel-blob

# --- Vercel Blob（IMAGE_STORAGE_PROVIDER=vercel-blob 時に必要） ---
BLOB_READ_WRITE_TOKEN=

# --- Google Cloud Storage（IMAGE_STORAGE_PROVIDER=gcs 時に必要） ---
GCS_BUCKET_NAME=
GCS_PROJECT_ID=

# ===========================================
# AI プロバイダ設定（Gemini）
# ===========================================

# K_SERVICE 環境変数の有無でプロバイダを自動切り替え:
# - K_SERVICE あり（Cloud Run上）→ Vertex AI（サービスアカウント認証）
# - K_SERVICE なし（ローカル/Vercel等）→ Google AI（APIキー認証）

# --- Google AI（ローカル開発・Vercel等、GCP外で必要） ---
GOOGLE_GENERATIVE_AI_API_KEY=

# --- Vertex AI（Cloud Runデプロイ時に必要） ---
GOOGLE_VERTEX_PROJECT=your-gcp-project-id
GOOGLE_VERTEX_LOCATION=global

# ===========================================
# ジオコーディング設定
# ===========================================

# 逆ジオコーディングプロバイダ（yahoo | nominatim）
# デフォルト: yahoo
GEO_PROVIDER=yahoo

# Yahoo逆ジオコーダー（GEO_PROVIDER=yahoo 時に必要）
YAHOO_GEOCODING_APP_ID=

# Nominatim利用時のUser-Agent（GEO_PROVIDER=nominatim 時に推奨）
NOMINATIM_USER_AGENT=AIRS/1.0 (wildlife-report-system)

# ===========================================
# Google Cloud Translation API（獣種名の自動翻訳）
# ===========================================

# 獣種設定で日本語名→英語キーの自動翻訳に使用
# GCPコンソールでCloud Translation APIを有効化し、APIキーを発行
GOOGLE_CLOUD_API_KEY=

# ===========================================
# LINE公式アカウント（任意）
# ===========================================

# 友だち追加QRコード画像のURL
LINE_FRIEND_QR_URL=

# 友だち追加リンク
LINE_FRIEND_ADD_URL=

# ===========================================
# アプリケーションモード
# ===========================================

# 管理者モード（1: 有効, 0: 無効）
NEXT_PUBLIC_ADMIN_MODE=0

# デモモード（1: 有効, 0: 無効）
NEXT_PUBLIC_DEMO_MODE=0

# ===========================================
# アプリケーションURL（任意）
# ===========================================

# LINE Bot通報完了メッセージの編集URL生成に使用
# 未設定の場合、VERCEL_PROJECT_PRODUCTION_URL → http://localhost:3000 の順で使用
APP_URL=

# ===========================================
# JWT トークン署名（必須）
# ===========================================

# 通報編集ページのアクセス制御用秘密鍵
REPORT_TOKEN_SECRET=your-secret-key-here
