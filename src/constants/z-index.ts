/**
 * z-index 一元管理定数
 *
 * アプリケーション全体のz-index階層を管理します。
 * 新しいz-indexを追加する際は、必ずこのファイルの定数を使用してください。
 *
 * 階層構造:
 * ┌─────────────────────────────────────────────────────┐
 * │ z-9999  モバイルドロワー（最前面）                    │
 * │ z-9998  モバイルオーバーレイ背景                      │
 * ├─────────────────────────────────────────────────────┤
 * │ z-5000  モーダル                                     │
 * │ z-4000  トースト通知                                 │
 * ├─────────────────────────────────────────────────────┤
 * │ z-2100  ドロップダウン・ポップオーバー                │
 * │ z-2000  スティッキーヘッダー                         │
 * ├─────────────────────────────────────────────────────┤
 * │ z-1120  地図レイヤーパネル（右ドロワー、最前面）        │
 * │ z-1110  タイムラインコントローラー                     │
 * │ z-1100  地図コントロール（ベースマップ選択、戻るボタン）│
 * │         ※ Leafletコントロール（800〜1000）より上      │
 * │ z-1050  地図データパネル（サイドパネルより下）         │
 * │ z-10    地図内の相対的な要素                         │
 * ├─────────────────────────────────────────────────────┤
 * │ z-0     通常コンテンツ                               │
 * │ z--10   背景要素                                     │
 * └─────────────────────────────────────────────────────┘
 *
 * Leafletのデフォルトz-index参考値:
 * - タイルレイヤー: 1
 * - マーカー: 200〜600
 * - ポップアップ: 700
 * - コントロール（ズームボタン等）: 800〜1000
 *
 * 重要:
 * - 地図コントロール（1100）〜レイヤーパネル（1120）はLeafletコントロール（〜1000）より上
 * - ヘッダー（2000）は地図オーバーレイ（1100）より上
 * - モバイルメニュー（9998-9999）は全ての要素より上に表示
 */

// ========================================
// 基本レイヤー
// ========================================

/** 背景要素 */
export const Z_BACKGROUND = -10;

/** 通常コンテンツ（デフォルト） */
export const Z_BASE = 0;

// ========================================
// 地図関連
// ========================================

/** 地図内の相対的な要素（パネル内ヘッダー等） */
export const Z_MAP_INTERNAL = 10;

/**
 * 地図データパネル（ReportDataPanel等）
 * Leafletコントロール（〜1000）より上、サイドパネル（1100）より下
 */
export const Z_MAP_DATA_PANEL = 1050;

/**
 * 地図コントロール（ベースマップ選択、戻るボタン等）
 * Leafletのコントロール（〜1000）より上に表示
 */
export const Z_MAP_CONTROLS = 1100;

/**
 * タイムラインコントローラー
 * 地図コントロール（1100）より上、レイヤーパネル（1120）より下
 */
export const Z_MAP_TIMELINE = 1110;

/**
 * 地図レイヤーパネル（右ドロワー）
 * 地図UI要素の中で最前面に表示
 */
export const Z_MAP_PANEL = 1120;

/** @deprecated Z_MAP_CONTROLS, Z_MAP_TIMELINE, Z_MAP_PANEL を使用してください */
export const Z_MAP_OVERLAY = 1100;

// ========================================
// グローバルUI要素
// ========================================

/** スティッキーヘッダー・ナビゲーション */
export const Z_HEADER = 2000;

/** ドロップダウンメニュー、ポップオーバー */
export const Z_DROPDOWN = 2100;

// ========================================
// オーバーレイ・モーダル
// ========================================

/** トースト通知 */
export const Z_TOAST = 4000;

/** モーダルダイアログ */
export const Z_MODAL = 5000;

// ========================================
// モバイルメニュー（最前面）
// ========================================

/** モバイルメニューオーバーレイ背景 */
export const Z_MOBILE_OVERLAY = 9998;

/** モバイルドロワー */
export const Z_MOBILE_DRAWER = 9999;

// ========================================
// インラインスタイル用のオブジェクト
// ========================================

/**
 * インラインスタイル用のオブジェクト
 * 例: style={{ zIndex: zIndexStyles.mapOverlay }}
 */
export const zIndexStyles = {
  background: Z_BACKGROUND,
  base: Z_BASE,
  mapInternal: Z_MAP_INTERNAL,
  mapDataPanel: Z_MAP_DATA_PANEL,
  mapControls: Z_MAP_CONTROLS,
  mapTimeline: Z_MAP_TIMELINE,
  mapPanel: Z_MAP_PANEL,
  /** @deprecated mapControls, mapTimeline, mapPanel を使用 */
  mapOverlay: Z_MAP_OVERLAY,
  header: Z_HEADER,
  dropdown: Z_DROPDOWN,
  toast: Z_TOAST,
  modal: Z_MODAL,
  mobileOverlay: Z_MOBILE_OVERLAY,
  mobileDrawer: Z_MOBILE_DRAWER,
} as const;
